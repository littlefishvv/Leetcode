本章全是关于数组的题目，说到数组，绕不开的就是各种查找和排序，尤其是二分查找和快速排序，应用非常广泛
然后就是各种套路，比如双指针(比如对撞指针)和循环不变量（其实也算是滑动窗口吧），或者动态规划，或者暴力法多重循环

其实完全可以做到看到数组的题目就想到双指针，看到排好序就想到二分查找，看到未排序就想到快速排序

26,27,80,283这四题是一类问题，全都是套路
75题是快速排序（三路快排）的思想 arrays.sort用的就是这个算法;
215也是快排，只是利用了循环不变量的快排，同时，也可以用优先队列来解决这个问题;
167,三路快排（两路快排）运用了对撞指针的思想;